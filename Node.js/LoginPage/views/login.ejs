<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>lOGIN</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5 col-md-6 border border-primary rounded-top rounded-bottom p-5">
        <h2 class="mb-4 text-center text-primary">Login Page</h2>
        <form action='/login' method="post">
            <div class="form-group">
                <label for="username">Email</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" class="form-control" id="password" name="password" required>
            </div>
            
            <button type="submit" class="btn btn-primary">Log In</button>
            <div class="pt-3">
                <% if(messages.error) { %>
                    <div class="alert alert-danger " role="alert">
                        <strong>Incorrect Email</strong>
                    </div>
                <% }
                 %>
    
            </div>
            <div class="pt-2">
                <p >Don't have an Account?<a href="register"> Creat account</a></p>
            </div>
        </form>
    </div>
</body>
</html>